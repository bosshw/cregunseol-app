# 폰 알림(웹 푸시) 켜는 순서 — 대표님용

앱 쪽은 이미 다 들어가 있습니다. 아래 **다섯 단계**는 Supabase 쪽 한 번만 해두시면 되는 일입니다.
중간에 막히시면 그 화면을 그대로 보여주시면 됩니다.

무엇이 오는지: **부화 예정(사흘 전·당일)** 과 **밥 주는 날**, 아침 8시에 묶어서 한 통입니다.

---

## 1단계 — 열쇠 한 쌍 만들기 (PC에서, 1분)

프로젝트 폴더에서 명령창을 열고:

```
node scripts/make-vapid.mjs
```

두 줄이 나옵니다. 이 창을 닫지 말고 다음 단계로 가세요.

> ⚠️ **VAPID_PRIVATE 는 비밀입니다.** 저장소·카톡·메모장에 올리지 마세요.
> 잃어버려도 괜찮습니다 — 새로 만들고 앱에서 알림을 껐다 켜면 됩니다. 기록은 아무 영향 없습니다.

---

## 2단계 — 표 두 개 만들기

Supabase 대시보드 → **SQL Editor** → `supabase_push.sql` 파일을 열어
**맨 위부터 "5. 매일 아침 8시에 보내기" 바로 앞까지** 복사해 붙여넣고 **Run**.

(5번은 3·4단계를 마친 뒤에 합니다.)

---

## 3단계 — 보내는 함수 올리기

Supabase 대시보드 → **Edge Functions** → **Deploy a new function**

- 이름: `push` (반드시 이 이름이어야 합니다)
- 파일 두 개를 만들어 각각 붙여넣습니다
  - `index.ts`   ← `supabase/functions/push/index.ts` 내용
  - `webpush.ts` ← `supabase/functions/push/webpush.ts` 내용
- **Verify JWT 는 꺼주세요** (함수가 스스로 확인합니다)
- **Deploy**

---

## 4단계 — 비밀값 넣기

Edge Functions → `push` → **Secrets** 에 네 줄을 넣습니다.

| 이름 | 값 |
|---|---|
| `VAPID_PUBLIC` | 1단계에서 나온 위쪽 줄 |
| `VAPID_PRIVATE` | 1단계에서 나온 아래쪽 줄 |
| `VAPID_SUBJECT` | `mailto:gkssnfdl@gmail.com` |
| `CRON_KEY` | 아무 암호나 길게 (예: 영문+숫자 30자). **5단계에서 똑같이 씁니다** |

---

## 5단계 — 아침 8시 예약 걸기

`supabase_push.sql` 의 **"5. 매일 아침 8시에 보내기"** 아래를 복사해서,
`여기에_아무_암호나_길게` 를 **4단계의 CRON_KEY 와 똑같은 값**으로 바꾼 뒤 SQL Editor 에서 Run.

확인:
```sql
select jobname, schedule, active from cron.job;
```
`cg-push-daily` 한 줄이 `0 23 * * *` 로 보이면 됩니다. (세계표준시 23시 = 한국 아침 8시)

---

## 다 됐는지 확인하기

1. 폰에서 앱을 열고 **설정 → 🔔 폰 알림 → 알림 받기**
2. 허용을 누르면 카드가 "● 이 기기로 알림을 받고 있어요" 로 바뀝니다
3. **📨 시험 알림** 을 눌러 잠금화면에 뜨는지 봅니다

---

## 안 될 때 짚어볼 곳

| 화면에 뜨는 말 | 뜻과 할 일 |
|---|---|
| 아이폰은 홈 화면에 추가한 뒤에… | **아이폰은 사파리 탭에서는 절대 안 됩니다.** 공유 → "홈 화면에 추가" 후 그 아이콘으로 열어주세요 (iOS 16.4 이상) |
| 먼저 서버 동기화를 연결해주세요 | 위쪽 ☁️ 카드에서 로그인부터 해주세요 |
| 알림이 차단돼 있어요 | 폰 설정 → 알림에서 이 앱을 켜주세요 |
| 서버에 알림 설정이 아직 안 돼 있어요 | 3·4단계(함수와 비밀값)를 아직 안 하신 상태입니다 |
| 시험 알림은 오는데 아침에 안 옴 | 5단계 예약을 확인하세요: `select * from cron.job_run_details order by start_time desc limit 5;` |

---

## 알아두시면 좋은 것

- **아이폰은 홈 화면에 추가해야만** 알림이 옵니다 (애플 제약, 우회 방법 없음). 테스터분들께 꼭 안내해 주세요
- 앱이 **무엇을 언제 보낼지 계산해서 서버에 올려둡니다**. 그래서 앱을 가끔은 열어주셔야 앞으로 석 달치 일정표가 새로 채워집니다
- 폰을 바꾸시면 새 폰에서 한 번 더 **알림 받기** 를 눌러주세요. 옛 폰은 앱을 지우면 자동으로 목록에서 빠집니다
- 비용은 들지 않습니다
