# 폰 알림(웹 푸시) — 어떻게 돌아가는지, 안 될 때 어디를 보는지

Supabase 쪽 준비는 **두 가지**뿐이고, 둘 다 단아가 대신 해뒀습니다.
이 문서는 나중에 무언가 이상할 때 **어디를 보면 되는지** 적어둔 것입니다.

## 뭐가 오는가

- **🥚 부화 예정** — 사흘 전, 그리고 당일
- **🍽️ 밥 주는 날** — 그날
- **아침 8시에 그날 몫을 묶어서 한 통.** 두 건이면 "오늘 챙길 것 2가지" 로 합쳐집니다

## 폰에서 켜는 법 (대표님과 테스터분들)

1. 앱 → **설정 → 🔔 폰 알림 → [알림 받기]**
2. 허용을 누르면 "● 이 기기로 알림을 받고 있어요" 로 바뀝니다
3. **[📨 시험 알림]** 을 눌러 잠금화면에 뜨는지 확인

> ⚠️ **아이폰은 홈 화면에 추가해야만 알림이 옵니다.**
> 사파리 탭으로 열면 절대 안 옵니다(애플 제약, 우회 방법 없음).
> 공유 버튼 → "홈 화면에 추가" → **그 아이콘으로 열기.** iOS 16.4 이상.
>
> ⚠️ **서버 동기화(로그인)가 켜져 있어야 합니다.** 어느 폰으로 보낼지 알아야 하니까요.

## 서버에 뭐가 들어가 있나 (이미 해둔 것)

| 무엇 | 어디 |
|---|---|
| 표 3개 + 아침 8시 예약 | `supabase_push.sql` 을 SQL Editor 에서 실행 |
| 보내는 함수 `push` | Edge Functions 에 `index.ts` + `webpush.ts` 배포 (Verify JWT 끔) |

**열쇠는 사람이 만들지 않습니다.** 함수가 처음 불릴 때 스스로 만들어
`cg_push_keys` 표에 넣어둡니다. 그 표는 **아무도 못 봅니다**(정책을 하나도 안 만들었습니다).
서버로 도는 함수만 읽습니다. 그래서 옮겨적다 흘릴 비밀번호가 없습니다.

## 안 될 때 짚어볼 곳

| 화면에 뜨는 말 | 뜻과 할 일 |
|---|---|
| 아이폰은 홈 화면에 추가한 뒤에… | 사파리 탭에서 보고 계십니다. 공유 → "홈 화면에 추가" 후 그 아이콘으로 여세요 |
| 먼저 서버 동기화를 연결해주세요 | 설정 위쪽 ☁️ 카드에서 로그인부터 |
| 알림이 차단돼 있어요 | 폰 설정 → 알림에서 이 앱을 켜주세요 |
| 서버에 알림 설정이 아직 안 돼 있어요 | 함수 `push` 가 안 떠 있습니다. Edge Functions 에서 상태 확인 |
| 시험 알림은 오는데 아침에 안 옴 | 예약을 확인하세요 (아래 SQL) |

```sql
-- 예약이 살아 있는지
select jobname, schedule, active from cron.job where jobname = 'cg-push-daily';
-- 최근 실행 결과
select status, start_time, return_message from cron.job_run_details order by start_time desc limit 5;
-- 알림 켠 기기 수
select label, hatch, feed, updated_at from cg_push_subs;
```

## 알아두시면 좋은 것

- 앱이 **앞으로 석 달치 일정표를 계산해 서버에 올려둡니다.** 그래서 앱을 가끔은 열어주셔야
  일정표가 새로 채워집니다(석 달치라 여유는 넉넉합니다)
- 폰을 바꾸시면 새 폰에서 **[알림 받기]** 를 한 번 더 눌러주세요.
  옛 폰은 앱을 지우면 목록에서 자동으로 빠집니다
- 알림 종류는 설정 카드에서 **하나씩 껐다 켤 수 있습니다**
- 비용은 들지 않습니다
